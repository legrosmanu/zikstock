# Test Environment Variables
# This file is loaded automatically when running tests

# Firestore Emulator Configuration
# Make sure the Firebase Emulator is running on this host before running tests
FIRESTORE_EMULATOR_HOST=localhost:8080

# Google OAuth2 Client ID used by the auth middleware to validate the JWT audience.
# In tests, jwks-rsa is mocked so this value is not verified against Google.
GOOGLE_CLIENT_ID=test-client-id
